export default function HomepageRecent({ data }) {
  return (
    <div className="homepage-recent">
      Last 5 created characters will go here?
      {/* <div className="favorites-tiny" key={c._id}>
              <img src="" alt={c.name} />
              <p>
                <strong>{c.name}</strong> <br />
                <strong>Level:</strong> {c.level} |  
                <strong>Class:</strong> {c.charClass}
              </p>
            </div> */}
    </div>
  );
}
